#include <iostream>
using namespace std;
int main()
{
    int N, suma = 0;
    cin >> N;
    for(int i = 0; N >= i; i++)
    {
        if(i%2 == 0)
            suma += i;
    }
    cout << suma << endl;
}
